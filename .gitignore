# User data generated when using ghostink — NEVER commit to the source repo.
# Leading slash ensures these patterns match only at the repo root, so
# examples/ and templates/ under the source repo are unaffected.
/style_spec.md
/style_init_log.md
/author_profile/
/drafts/
/analyzed_authors/
/illustrate_config.md

# macOS
.DS_Store

# Claude Code local settings
.claude/settings.local.json
